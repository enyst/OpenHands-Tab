# agent-sdk-ts parity notes

## Context
- The TypeScript SDK docs describe a VS Code-focused implementation with local and remote conversation paths and a layered runtime/tooling stack.【F:docs/agent-sdk-architecture.md†L1-L159】
- The package-level guidance still calls `LocalConversation` a stub even though the code now runs an orchestration loop with EventLog and tools.【F:packages/agent-sdk-ts/AGENTS.md†L13-L133】【F:packages/agent-sdk-ts/src/sdk/conversation/LocalConversation.ts†L22-L186】

## Gaps vs the Python agent-sdk
- **State persistence and recovery**: Python `LocalConversation` initializes `ConversationState` with an on-disk persistence directory and wires callbacks/visualizers before the loop runs, while the TypeScript version keeps an in-memory `EventLog` with no persistence or state hydration hooks.【F:openhands-sdk/openhands/sdk/conversation/impl/local_conversation.py†L104-L149】【F:packages/agent-sdk-ts/src/sdk/conversation/LocalConversation.ts†L37-L186】
- **Stuck detection and telemetry**: The Python runtime attaches `StuckDetector`, conversation stats, and optional visualizers during setup; the TypeScript loop never instantiates these helpers despite having a `StuckDetector` utility in the runtime layer.【F:openhands-sdk/openhands/sdk/conversation/impl/local_conversation.py†L146-L189】【F:packages/agent-sdk-ts/src/sdk/conversation/LocalConversation.ts†L22-L186】
- **Secrets and LLM registry**: Python maps provided secrets into the registry and subscribes LLMs to a registry/stats pipeline, whereas the TS `SecretRegistry` only probes env/VS Code SecretStorage and is not connected to conversation lifecycle hooks.【F:openhands-sdk/openhands/sdk/conversation/impl/local_conversation.py†L152-L163】【F:packages/agent-sdk-ts/src/sdk/runtime/SecretRegistry.ts†L12-L41】
- **Workspace/tool negotiation for remote mode**: Python exposes remote and local workspaces with paged history fetching; the TS `RemoteConversation` always starts a `LocalWorkspace` session with a fixed tool list and does not let callers pass RemoteWorkspace-style options.【F:openhands-sdk/openhands/sdk/conversation/impl/remote_conversation.py†L12-L199】【F:packages/agent-sdk-ts/src/sdk/conversation/RemoteConversation.ts†L143-L184】

## Next steps to close parity and VS Code readiness
1. **Align docs with reality**: Update `packages/agent-sdk-ts/AGENTS.md` (and the architecture guide) to reflect that `LocalConversation` now runs an agent loop but lacks persistence/stuck-detection wiring; call out any VS Code-only constraints explicitly for extension authors.【F:packages/agent-sdk-ts/AGENTS.md†L13-L133】
2. **Add persistence hooks to the TS runtime**: Introduce a persistence layer analogous to Python's `ConversationState.create(...)` so `EventLog` snapshots can be restored between VS Code sessions, and expose a persistence path in `LocalConversation` options.【F:openhands-sdk/openhands/sdk/conversation/impl/local_conversation.py†L104-L149】【F:packages/agent-sdk-ts/src/sdk/conversation/LocalConversation.ts†L37-L186】
3. **Wire stuck detection/telemetry**: Instantiate `StuckDetector`, conversation stats, and optional visualizers inside `LocalConversation` and surface events so VS Code can show health/state indicators, matching the Python runtime behavior.【F:openhands-sdk/openhands/sdk/conversation/impl/local_conversation.py†L146-L189】【F:packages/agent-sdk-ts/src/sdk/conversation/LocalConversation.ts†L22-L186】
4. **Integrate secrets and LLM tracking**: Let the extension inject VS Code `SecretStorage` into `SecretRegistry`, add registration methods for per-conversation secrets, and mirror the Python LLM registry/stats plumbing so usage data is available to the webview and logs.【F:openhands-sdk/openhands/sdk/conversation/impl/local_conversation.py†L152-L163】【F:packages/agent-sdk-ts/src/sdk/runtime/SecretRegistry.ts†L12-L41】
5. **Expand remote-conversation options**: Allow callers to specify remote workspaces/tooling and pass through the agent-server capabilities (e.g., MCP/skills) instead of the fixed local workspace + terminal/file/task trio, keeping history replay intact.【F:openhands-sdk/openhands/sdk/conversation/impl/remote_conversation.py†L12-L199】【F:packages/agent-sdk-ts/src/sdk/conversation/RemoteConversation.ts†L143-L184】
